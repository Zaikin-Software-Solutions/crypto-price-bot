# Production compose: pulls the prebuilt multi-arch image from GHCR.
# For local development with rebuild-on-change, use:
#   docker compose -f docker-compose.yml -f docker-compose.build.yml up -d --build
services:
  crypto-price-bot:
    image: ghcr.io/zaikin-software-solutions/crypto-price-bot:latest
    container_name: crypto-price-bot
    restart: unless-stopped
    env_file:
      - .env
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
